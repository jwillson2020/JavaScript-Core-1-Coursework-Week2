
function sortArray(letters) {
    let sortedLetters = letters.sort();
  
    return sortedLetters;
  }

  var sortArray = ["a", "n", "c", "e", "z", "f"];

  console.log(sortedLetters); // not working